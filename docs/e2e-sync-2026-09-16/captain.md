# Captain preferences

## Orchflows
- Run every ship and scout through orch-dynamic-workflow unless I name another workflow or say "plain". Run any other workflow only when I name it: invoke it by its slash command or read its SKILL.md by path.
- Resolve each agent's model and effort per /home/danhm/orchflows-e2e/home/projects/orchflows-home/.local/packages/orchflows-firstmate/docs/architecture.md#model-and-effort: my request, then the saved workflow, then the Orchflows rules in crew-dispatch.json, then your effort fallback.
- Record each workflow's phase and task IDs in the backlog item note and resume from it after a restart.
- Read /home/danhm/orchflows-e2e/home/projects/orchflows-home/.local/packages/orchflows-firstmate/docs/firstmate.md before the first Orchflows dispatch of a session.
- Never register Orchflows libraries with a host (no plugin install, marketplace add, or --plugin-dir change); run a named library workflow such as quickfix:fix by reading its SKILL.md from /home/danhm/orchflows-e2e/home/projects/orchflows-home/libraries/<library>/skills/<skill>/SKILL.md. (2026-09-16)
