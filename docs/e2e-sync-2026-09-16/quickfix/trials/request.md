# Trial request

Project: `roman`.

Workflow: `quickfix:fix`, named explicitly by the captain.

Bounded request given to the workflow:

> Add a `--lower` flag so that output numerals are printed in lowercase, and lowercase input is accepted only when the flag is given. Include tests and a README line.

This is the exact captain's-intent text passed into the Work assignment (step 1 of `fix`). No other scope: no other flags, no unrelated refactors, no other README changes.
