# Captain preferences

## Orchflows
- Run every ship and scout through orch-dynamic-workflow unless I name another workflow or say "plain". Run a custom workflow only when I name it.
- Resolve each agent's model and effort per /tmp/orchflows-e2e/home/projects/orchflows-home/.local/packages/orchflows-firstmate/docs/architecture.md#model-and-effort: my request, then the saved workflow, then the Orchflows rules in crew-dispatch.json, then your effort fallback.
- Record each workflow's phase and task IDs in the backlog item note and resume from it after a restart.
- Read /tmp/orchflows-e2e/home/projects/orchflows-home/.local/packages/orchflows-firstmate/docs/firstmate.md before the first Orchflows dispatch of a session.
