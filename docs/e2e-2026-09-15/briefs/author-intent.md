Create a reusable Orchflows workflow library named mixed-build with one workflow, feature, that builds a small software feature in three phases: spec, implement, docs-qa. Each phase is one Work, then one independent Review by a fresh agent, then one repair pass. Alternate Claude Sonnet 5 at effort xhigh and Codex gpt-5.6-luna at effort xhigh so that every reviewer is a different vendor from its maker. Save those model and effort preferences in the workflow. The workflow runs only when I name it.