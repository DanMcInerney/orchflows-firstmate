# Plug-and-play implementation verification

Dev.9 implements assignment controls, a saved-workflow home and ordinary
selection, and scoped composition through existing FirstMate owners. **326
package/integration tests and 38 observer tests pass**, with ten explicit native
Windows skips. Two actual Claude roots created, saved and reused a composed
workflow through seven component requests. **Qualified delivery and cleanup
passed.** Original failed receipts remain failed; this was not an uninterrupted
or prompt-free acceptance run.

See the [implementation contract](plug-and-play-implementation.md) and
[exact state](plug-and-play-state.json). Development began at
`1e41a6d0e9dd4880389a23989ffa36c78cad5b9c`; existing reassessment edits were
preserved. Active installations, pinned research references, the original dated
assessment and historical runtime receipts remain unchanged.

## Source and ownership

FirstMate remains pinned to `b182d0f908b78d08c7ccb8dce3775bdca8c5d657` and
Orchflows to `ca72258493480ddcfe73b3f01d0475ad532e4726`. All 1,081 upstream
paths remain present. The package has 1,092 files; the distribution inventories
27 deployables including nine sequential patches. Preparation reproduces a
571-path candidate including one symlink.

FirstMate retains fleet launch, worktree, inbox, supervision, recovery and local
landing authority. Assignment profiles reuse its flag owner. Saved libraries
reuse package setup/resolve and retained bundle inputs. Selected call scopes
authorize each Review and one level of named writable Work descendants. No new
scheduler, launcher, lifecycle service or direct Herdr adapter was introduced.

## Joined Linux checks

The final prepared check `check-58qx6kf0` passed **135 package and 191 Linux
integration tests: 326 passed**, with ten explicit native Windows skips and no
unexpected skips. These are fixtures, separate from native workers. Coverage
includes:

- Independent model/effort precedence, accepted-profile replay and provenance,
  changed/missing profile refusal, legacy negotiation and Claude/Codex flag
  translation. Thirteen existing effort mappings preserve the original owner.
- Complete committed-library publication, hidden default/explicit home,
  identity selection, capability and name validation, future bundle refresh,
  active snapshot retention and local default-branch selection.
- Separately reviewed ordered dynamic calls, no reopened phase, bounded named
  writable descendants, caller-specific gathering and existing lifecycle custody.
- Create/save/reuse through ordinary brief and local merge owners, with injected
  dispatch explicitly distinguished from workers. Real public client/controller
  status and replay are included.
- A real public launch-position callback under existing spawn/project locks and
  Treehouse slot claims, with local main ahead of origin. Dirty, foreign, primary,
  named-branch and unowned targets refuse; published roots preserve their work.

The first join stopped at a deployable hash guard during parallel edits. The
next found two installed-package documentation links escaping the package and
one stale capitalization assertion. The pre-review check then passed 319 tests;
the first repaired check passed 322. Original receipts remain under ignored
`.scratch/plug-and-play/checks/`. Exact input and candidate hashes are in the
state record. The final source matches the prepared product snapshot.

## Independent development Review and repair

One fresh independent Review inspected the joined source and passed 27 targeted
tests. It identified three defects:

1. The public protocol omitted both new capabilities, so the real client refused
   before dispatch.
2. An incompatible read-only Work could reserve a declared descendant caller ID
   and then have no valid completion path.
3. Invalid committed library/skill names could be installed and poison catalog
   refresh.

The post-review repair/check phase adds public capability advertisement and real
client coverage, rejects incompatible callers before reservation, and uses the
package's naming validation before publication writes. A later native reuse
attempt exposed a fourth concrete gap: ordinary pool freshening selected the
older origin commit after local-only delivery advanced local main. The existing
launch-position owner now restores a fresh root's retained input under its
existing custody checks. No second independent Review ran.

## Actual create/save/reuse evidence

The useful artifact is a complete `release-triage:ship` library. Its script
classifies real incident input, and its saved skill composes separately reviewed
classify and brief phases in the caller context.

| Step | Actual result |
| --- | --- |
| Author | One writable Work created the seven-file library. A fresh read-only Work ran its script and four tests. One independent Review found no discrepancies. Exact reports/results and trial JSON were committed. |
| Save | Ordinary local delivery landed `0f22dff5202a28a7c93dac5c8b9978c671e25912`; FirstMate published the committed library and resolved its identity from the workflow home. |
| Reuse | Ordinary brief selection launched a new ship/local-only root. Classify Work/Review completed and was gathered before brief Work/Review. Both reviewed the actual joined outputs. |
| Update | While v1 was active, FirstMate published v2. All five checks passed: the running attachment/instructions stayed v1 and a future ordinary spawn callback retained v2. That callback launched no third root. |
| Deliver | Qualified checks passed for exact evidence, full native reads before gather, replay, profiles, two independent scoped Reviews and expected release output. Ordinary local delivery landed `a942ff90bd2d743d64aeaeb4ea2ce78fc1b22915`. |
| Cleanup | Ordinary teardown succeeded. All 31 tracked files were exported from regular Git blobs and checked against their blob identities. Output refs resolve, both root metadata records are removed, origin was never pushed and zero scoped processes remain. |

Native process arguments and current-run transcripts confirm `claude-sonnet-5`:
roots and four Work assignments used high effort; three Reviews used medium.
Both the model and effort fields reached native launches, but a different model
was not exercised. Native replay evidence shows two matching submissions for
each of the seven requests. Full report/result reads preceded gathering. The
final delivery-only assessment launched no model and used no authentication.

### Qualification and original failures

- The initial live attempt `a-j6aevmd2` stopped on the missing public capability
  before any component. The original observer's absent-file error and refused
  unfinished-root teardown remain recorded; zero processes remained.
- In `a-13p2cmfd`, the authoring root completed sixteen checks but the first
  observer missed replay responses saved as JSON and printed as complete
  child/profile projections. It withheld landing. A separate assessment verified
  those projections and delivered the unchanged ready commit.
- Two administrative startup attempts stopped before any reuse worker because
  the original observer rejected a normal successful watcher rearm wake. The
  correction requires the exact arm PID's successful owner ledger record. A
  subsequent prelaunch refusal exposed the real origin/local-main base mismatch
  described above; the retained-input guard stayed intact through its repair.
- The actual reuse root completed fifteen checks and signaled ready, but its
  observer missed another valid projection: child, body hash and profile hash.
  The corrected observation independently recomputes the controller's request
  and profile hashes, binds both to that accepted child, and confirms two native
  submissions. Current-run model observation excludes older transcripts from
  reused pool slots. Full-read and artifact requirements are unchanged.
- One native Claude outside-directory read prompt was confirmed through
  FirstMate's existing key owner for a retained candidate file, affecting only
  the disposable Claude configuration. This is not prompt-free operation.

The final separate receipt is `qualified-delivery-passed`: eighteen author checks,
seventeen reuse checks, five publication checks and all cleanup checks pass.
The same private lab was restored only for the existing owners' endpoint-absence
and delivery/teardown checks; no stopped task endpoint or worker was recreated.
All original failed receipts, driver versions, native traces, separate
qualifications and delivered files are preserved under ignored
`.scratch/plug-and-play/`. The last observer suite has **38 passing tests**.
Successful one-shot watcher notification is not continuous-supervision evidence.

## Remaining boundaries

Current native Codex composition and model availability, different actual models,
nested composing Build trials, general SelfImprove, unrestricted recursion,
unrelated project shapes, tracked symlink/submodule inputs, remote homes,
direct-PR/no-mistakes delivery, installation/update/rollback and full optional
library parity remain open. Native Windows is deferred. A fresh script trial
alone does not establish composing Build parity. Existing qualified dev.8
leaf-repair evidence and strict failures remain unchanged.
