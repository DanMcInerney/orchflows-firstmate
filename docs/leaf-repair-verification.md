# Leaf repair-trial delivery continuation

This continuation starts from 4f63714d4497ff5ba11381b5774ffa75032958ab.
FirstMate remains pinned to b182d0f908b78d08c7ccb8dce3775bdca8c5d657 and the
retained Orchflows source to ca72258493480ddcfe73b3f01d0475ad532e4726.
Package dev.8 and the prepared FirstMate candidate are unchanged.

## Implemented scope

The package's final dev.8 Build/client clarification already required committing
additional repair-trial evidence before readiness. A separate
[bounded acceptance case](leaf-authoring.md#separate-post-review-repair-case)
now exercises that requirement through the existing dynamic local-only owners.
It adds no product capability, protocol field, lifecycle owner or harness.

The four-request case retains the original trial evidence, the Review's exact
report/result, and the fresh repair trial's output/result/provenance. Its final
library must equal the repaired trial input, with only the five declared
evidence files added afterward. The observer checks original and repaired
provenance, native full reads, request roles/order, replay, final checks after
repair gathering, and ordinary local landing and cleanup.

The original --authoring fixture remains strict. Its historical failures are
unchanged; the new case records the old unchanged-library assertion separately
and expects it to fail because the library was deliberately refined.

## Joined checks and independent development Review

Fresh Linux preparation check-q38fxj_o passed 128 package tests and 155 Linux
integration tests, with ten explicit native Windows skips. Non-live fixture
a-pjp8f5mg prepared and cleaned up successfully without authentication or models.
The initial driver suite passed 36 checks.

One fresh independent development Review independently passed those 36 checks
and found a P2 durability gap: the observer followed evidence-file symlinks,
allowing external bytes to appear committed even though Git retained only a
link target. Its disposable reproduction made all five new evidence files
external symlinks and passed the repair verifier.

The single repair/check pass now verifies regular Git blob modes and compares
committed blob identities with actual evidence bytes before landing. The same
owner covers the three original trial files, the five new files and the
post-cleanup check. It also checks regular blobs at the original reviewed
snapshot. Regressions reproduce external symlinks and uncommitted replacement
bytes. All 39 driver checks pass: **322 passed checks in total**, with the ten
explicit Windows skips. No second development Review ran.

## Actual worker verification

Trial **a-t27bkjzs** used explicit Claude Sonnet 5/high. Native assistant model
fields confirm claude-sonnet-5. The author returned a complete nine-file library;
the root joined its commit, and a fresh read-only leaf Work returned the correct
JSON. FirstMate replaced the root during that Work. The replacement preserved
accepted/gathered identities, reread its guidance, committed the original
output/result/provenance, and obtained one Review of
340319a49d1a509dacd82cebd81d841e94dc49a6.

The Review found no required repairs. The root made the fixture's explicit
ordering clarification, committed the changed library, and ran the fresh
repair-trial-v1 Work at that input. It returned the same correct JSON. The root
committed the five additional evidence files in
76a1ca904c6b4eb8e2fefdb5a4e0ee2c19463160 and signaled the expected ready branch.
All four requests passed native full-read, gather-order, replay and context
checks. Original and repaired library identities and retained evidence matched.
No operator intervention or additional Review occurred during the worker run.

**The original strict receipt failed.** The root prepended a separate
`cd <its exact recorded worktree>` line to the required final test command.
Its actual tests passed after repair gathering, but this violated the fixture's
standalone-call requirement. Both strict final-command assertions correctly
failed; landing was skipped, and cleanup preserved the unlanded branch.
No scoped processes remained. This was not a clean acceptance pass.

## Qualified assessment, ordinary landing and cleanup

The live run began before the development Review's symlink repair. Its original
receipt and exact driver source are preserved. A separate read-only assessment
used the final observer, proving all eight evidence files are regular Git blobs
containing the actual retained bytes, with the original Review/trial evidence
unchanged.

An explicitly selected reassessment recognizes only the literal final test
command preceded by exactly one `cd` to the same worktree recorded in the native
event. It verifies success and timing after repair gathering. Wrong directories,
variables, additional commands, early checks and failed results are rejected by
regressions. The normal acceptance driver keeps its strict default. The separate
receipt retains both failed strict assertions and labels the alternative
observation; it does not convert the original fixture to a pass.

All 51 qualified delivery checks, including ordinary landing, passed. The same
private lab was restored through its existing owner for structured endpoint
absence checks; no task endpoint, task record or worker was recreated.
FirstMate's existing fm-merge-local fast-forwarded main to the exact ready commit,
then ordinary root teardown and private-lab/sentinel cleanup succeeded.

After teardown, the delivered checkout is clean at that commit, all four
delivered tests pass, all eight evidence files retain their exact bytes and
regular Git blob identities, the author output ref survives, root metadata is
removed, and zero scoped processes remain. This closes the observed
repair-trial evidence retention and local delivery gap with a **qualified
assessment and administrative delivery resume**, not an uninterrupted strict
fixture pass. The follow-up launched no additional model.

[Exact state](leaf-repair-state.json) records identities, separate receipt
hashes, all checks and the preserved artifact bundle. Earlier dev.8 failed
receipts and their protected unlanded branches remain unchanged in
[leaf authoring verification](leaf-authoring-verification.md).

## Limits

This is same-project, non-delegating leaf authoring and one bounded post-Review
repair Work. It does not establish portability, composing or nested Build,
SelfImprove, current Codex composition, general recovery after endpoint cleanup,
direct-PR/no-mistakes delivery or production readiness. Active installations,
credential caches, pinned research sources and the dated assessment are unchanged.
