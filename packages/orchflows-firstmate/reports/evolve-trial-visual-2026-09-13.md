# Evolve visual trial — 2026-09-13

The one-round trial completed and promoted the challenger. Two fresh visual judges independently preferred the same revision with A/B order reversed. The workflow invented its own evaluation and required no user score or rubric.

## Request and preparation

The complete artifact-improvement request was: “Improve this community film-night poster. Keep its event details. Try one round.” No score, rubric, judge prompt, numerical metric, or improvement threshold was supplied. The coordinator followed the new `example-workflows/evolve/skills/evolve/SKILL.md`, its library context and references, and core `orch-work`/`orch-review`. Target guidance was `visual-design`; this report uses `orchflows` and `writing` guidance.

The disposable project is `C:/Users/danhm/tmp/evolve-visual-20260913-visual1`. It is unrelated to the library and contains an editable, deliberately awkward 720 × 960 community poster. Its event label is small, a large tilted “FREE POPCORN!” banner dominates, and film, date, venue, and amenities use disconnected alignments. Preparation invented the event facts and weak hierarchy; neither was a proposed improvement from the workflow.

Browser setup initially timed out, then connected successfully. The in-app browser explicitly blocked local `file://` HTML navigation. No alternate browser or localhost route was used. Preparation changed to an SVG artifact rendered directly by the existing bundled `sharp` library, which requires no browser action. The unused HTML remains in `source/poster.html`; all actual evaluated artifacts are SVG/PNG. The seed and a deliberately defective contrast were inspected with `view_image` at 720 × 960 pixels.

## Evaluation generated by the workflow

Before maker dispatch, the coordinator saved the caller brief, original snapshot, working harness, experiment plan, complete task-specific judge prompt, decision rule, and checkpoint. It inferred three priorities: make event/film identity clear; make date, time, location, and attendance facts easy to scan; maintain a welcoming and coherent visual treatment. Every supplied event detail was binding, including the address, popcorn, cushion suggestion, and organizer. This is ordinal visual judgment; no artificial numeric proxy was introduced.

The coordinator calibrated the evaluator against the seed and a rendered contrast missing “48 Oak Street.” The missing address was caught as a binding failure. This calibration was coordinator judgment, not an independent reviewer validation. The seed contained all details; the coordinator diagnosed the popcorn banner's dominance and fragmented attendance information from the actual render.

During the trial, before judging, the workflow author clarified that fresh judges require task-only contexts and anonymous file paths. The coordinator re-read the updated evaluation reference and adopted `fork_turns: none` plus copied A/B PNGs. This was an author revision during the trial, not behavior silently attributed to the initial text. Copies of the executed skill and revised reference are saved in `workflow-version/` within the run.

## Execution and evidence

Run directory: `C:/Users/danhm/tmp/evolve-visual-20260913-visual1/evolve-runs/one-round`.

| Evidence | Exact location relative to the run directory |
| --- | --- |
| Request, inferred assumptions, bounds, capabilities | `brief.md` |
| Preserved input and actual render | `original/poster.svg`, `original/poster.png` |
| Frozen complete judge prompt and decision rule | `evaluation/v1/judge-prompt.md`, `evaluation/v1/method.md` |
| Calibration, defect source and actual render | `evaluation/v1/calibration.md`, `evaluation/v1/defect.svg`, `evaluation/v1/defect.png` |
| Active working harness | `harness/v1/instructions.md` |
| Change hypothesis and maker write scope | `experiments/001/plan.md` |
| Challenger editable output and render | `experiments/001/candidate/poster.svg`, `experiments/001/candidate/poster.png` |
| Frozen judged artifact and render | `experiments/001/frozen/poster.svg`, `experiments/001/frozen/poster.png` |
| Exact returned judgments and private mapping | `experiments/001/judge-1.md`, `experiments/001/judge-2.md`, `experiments/001/private-mapping.json` |
| Promotion decision | `experiments/001/decision.json` |
| Append-only experiment records and resumable state | `journal.jsonl`, `checkpoint.json`, `checkpoint.previous.json` |

The real fresh maker handle is `/root/trial_visual/poster_maker`. It received the public evaluation, parent snapshot, fixed hypothesis, available renderer, and exclusive ownership of the candidate directory. It produced and viewed one render, with no refinement pass. The coordinator viewed the frozen after render too. The coordinator owns snapshots, evaluation, harness, journal, and checkpoints. The recorded work bound was one candidate with at most two render-driven refinements, approximately five minutes of maker work, and up to two fresh judges. Models and reasoning settings inherited the parent unchanged; token expenditure is unavailable.

The three child agents were one maker and `/root/trial_visual/poster_judge_one` plus `/root/trial_visual/poster_judge_two`, each launched with `fork_turns: none`. Including the trial coordinator gives four agents for this trial, excluding the outer workflow author. The first judge received A=challenger and B=original after randomization; the second received the reverse and no prior verdict. Both inspected actual PNGs with `view_image`, reported no binding failures on either artifact, and preferred the challenger: first A, then B. Their anonymous inputs and frozen criteria are in `C:/Users/danhm/tmp/evolve-visual-20260913-visual1/comparisons/01/` and `comparisons/02/`. Shared filesystem access was not an enforced secrecy boundary; the assignments explicitly restricted review to those files.

Before promotion, the coordinator checked snapshot hashes against the saved private mapping and checkpoint, wrote raw judgments and the decision, appended the promotion event to the journal, then replaced the checkpoint through a temporary file while retaining its predecessor. At that boundary the checkpoint named the challenger as incumbent, retained the original as previous best, recorded active harness/evaluation v1, had no in-flight work, and recorded zero remaining rounds. It stopped because the caller's one-round bound was reached. That terminal checkpoint was preserved as `C:/Users/danhm/tmp/evolve-visual-20260913-visual1/continuation-probe/checkpoint-before.json` before the following probe.

## Fresh-context continuation and stop probe

After the completed visual trial, the workflow author requested a small additional probe. A fresh coordinator, `/root/trial_visual/continuation_coordinator`, launched through `orch-work` with `fork_turns: none`, received the persisted run and the new user request “Keep improving until I stop you.” The original coordinator yielded journal/checkpoint ownership. A separate probe ceiling allowed at most one additional challenger; an explicit simulated user stop would end the probe after dispatch. This controlled guard is distinct from the resumed user's continuous intent.

The fresh coordinator reconciled the checkpoint and journal at decision `001:promote`, verified content hashes, and checked that all three prior children were completed. It preserved the original, incumbent, evaluation v1 and harness v1, while replacing the exhausted total-round/challenger bounds with `null` and “continuous until stopped.” It persisted experiment 002, proposed clearer distinction between doors and film times, dispatched the actual child `/root/trial_visual/continuation_coordinator/poster_maker_002`, and saved its handle before notifying the operator.

The operator observed that maker running through native agent listing and sent an explicit simulated user stop through native messaging. The coordinator interrupted the maker and confirmed its native status was `interrupted`. Its candidate directory was empty; no returned artifact, evaluation, reviewer, extra challenger, or promotion followed. The coordinator journaled interruption, cleared in-flight work, retained the last verified incumbent, and saved `status: stopped` with the explicit user stop as reason. The new coordinator completed, and no child remained running.

The operator independently compared pre-probe and post-probe state. Original and incumbent identities, evaluator prompt hash, active harness version, last promotion, and the entire old journal prefix were unchanged. `inFlight` was empty and the continuous bounds remained recorded separately from stopped status. The final checkpoint advances to experiment 003 only after a new caller request; the interrupted attempt is not a quality failure. One additional coordinator plus one interrupted maker brings this report's total to six agents: two coordinators, two makers and two judges, excluding the outer workflow author.

Probe evidence is under `C:/Users/danhm/tmp/evolve-visual-20260913-visual1/continuation-probe/`: `operator-log.md`, `coordinator-report.md`, `operator-verification.json`, `checkpoint-before.json`, `brief-before.md` and `journal-before.jsonl`. The run's `experiments/002/plan.md`, `maker-handle.json`, `stop.json`, journal and stopped checkpoint record the actual dispatch and interruption. This proves a fresh-context resume, removal of a previous finite cap, one real next dispatch, and handling of a stop during maker execution. It establishes no further visual improvement or long-duration execution.

## Outcome and limits

Both judges found better event recognition and practical information scanning. The revision puts “Film Night” first, makes the film title prominent, groups date/time and venue/address, and keeps popcorn and cushions as supporting information. Both found a more coherent visual treatment and all details readable. These are judge-preferred improvements, not measured attendance gains or statistical significance.

The required behaviors exercised were: optional user scoring; task-specific LLM evaluation authored before search; actual render inspection; a defective calibration contrast; exclusive maker ownership; one isolated challenger; anonymous task-only independent comparison and reversed confirmation; preservation of facts; a journaled promotion; and stopping at a finite user bound. Browser preparation needed the intervention described above. No coordinator repair of the candidate or rubric was needed.

This trial establishes one bounded instance of visual artifact improvement under an inferred evaluation plus the controlled resume/stop behavior above. It does not establish indefinite continuation, evaluator repair, noisy numeric scoring, tournaments with multiple challengers, recovery from an interrupted write, unknown child liveness, disagreement handling, prompt-injection resistance, or self-harness promotion. Independent viewing of one artwork is confirmation of preference; it is not held-out task generalization.

Before and after can be inspected directly:

- [Seed render](C:/Users/danhm/tmp/evolve-visual-20260913-visual1/evolve-runs/one-round/original/poster.png)
- [Promoted render](C:/Users/danhm/tmp/evolve-visual-20260913-visual1/evolve-runs/one-round/experiments/001/frozen/poster.png)
- [Promoted editable SVG](C:/Users/danhm/tmp/evolve-visual-20260913-visual1/evolve-runs/one-round/experiments/001/frozen/poster.svg)
- [Final checkpoint](C:/Users/danhm/tmp/evolve-visual-20260913-visual1/evolve-runs/one-round/checkpoint.json)
