# Evolve continuation and harness trial — 2026-09-13

Completed: two real resumed rounds retained original artifact a0 and active harness h1. The workflow reconciled a prepared interrupted checkpoint, compared actual old/new harness outputs, rejected conflicting evidence, used h1 in a later maker assignment, and stopped at the requested bound. It established no artifact or harness improvement.

## Request and declared context

The delegated request was: “Resume the existing public-notice improvement campaign and improve the process used to produce useful notices over the two remaining rounds.” The coordinator could choose its evaluator treatment and experiments; no winning technique or numeric score was requested. It received the current workflow and reference paths, core `orch-work`/`orch-review`, `orchflows` and `writing` guidance, the project/run locations and available local text tools/native children. It was required to write only in the disposable project.

Project: `C:/Users/danhm/tmp/evolve-harness-20260913-0024/`. Run: `evolve-runs/notices/` inside that project. The package was read by absolute path, without claiming native skill registration. Trial supervisor: `/root/trial_harness`; fresh coordinator: `/root/trial_harness/resume_campaign`, launched with `fork_turns: none` at approximately 04:26 UTC.

The supervisor followed `orch-work` under `orch-build-workflow` and applied the Make guidance for `orchflows` and `writing` to the trial and report. The coordinator received those paths and the complete current evolve package context. Trial preparation and reporting are overhead outside the campaign's recorded child count.

## Synthetic author preparation

`AUTHOR-PREPARATION.md` in the disposable project records the deliberately prepared starting state. All prior history, draft notices and event facts are synthetic author fixtures. The starting harness, h1, is a plausible one-pass writing prompt: lead with the disruption and practical alternative, remove repetition, prefer a heading and compact paragraphs, and aim for roughly 45 words where possible while preserving source meaning. Its absence of an explicit completeness check is associated with an authored failed draft; no earlier native agent run demonstrated that association.

The target artifact is a notice about a children's-room repainting closure, temporary book shelves, relocated storytime and unchanged services. The existing a0 notice preserves the source facts. A separate prior entrance-closure draft omits the step-free west ramp and operating lift. That failure is synthetic process memory, not a measured incumbent-versus-challenger result for the children's-room target. The fixture's committed `e0001` rejection retains a0 and consumes one of a three-round allowance, while the deliberately older checkpoint has no committed decision and still lists three rounds. No old native handle is live or unknown. This exercises journal reconciliation without needing to invent child liveness.

The author also prepared an inferred starting evaluator v1: factual/service-access requirements followed by readability and useful concision, ordinal A/B preference, and two independent judgments for subjective promotion. The coordinator may keep it or repair it in a separate experiment. This trial therefore tests use or repair of an existing evaluator; it does not establish evaluation design from scratch. Three reserved briefs precede the proposer: a known entrance-access failure, an author-described successful book-sale notice, and a fresh printing-service notice. “Fresh” means absent from this small synthetic history, not a sampled unseen real-world task.

The saved per-case work allowance is one native maker turn, at most two file-read tool calls and one candidate write, no maker delegation or web, with equal inherited model settings, guidance, source and starting draft for old/new arms. It allows one proposer, six case makers, two judges of the three pairs, one ordinary maker and up to two ordinary judges. The coordinator may report insufficient evidence instead of exceeding the allowance. The constraint is author trial preparation, not a prescribed scoring outcome or harness recipe.

## Workflow identity

The parent clarified anonymous judging before this coordinator was dispatched: task-only judge contexts without inherited maker/coordinator transcripts, anonymous artifact paths, and a caveat that shared filesystem access is not a secrecy boundary. The coordinator's initial assignment directed it to read the current reference. This trial starts with that clarified revision; it does not test the earlier version or a secrecy boundary supplied by the host.

| Loaded input | SHA-256 |
| --- | --- |
| `skills/evolve/SKILL.md` | `8C4E69E630D303503017E6F1E05BE4F16138D3A241E402126D64EB3A2EADA85F` |
| `skills/evolve/references/evaluation.md` | `D8503A7C5C3BC0E8D3E340175E7E50547FA4B85603BAD5B5834BDA08AAF04BC9` |
| `skills/evolve/references/state.md` | `866E9C8E5AC37B9FEAEF157D7541FC315E3BEE9E9EE0CD903DF420716C994FC8` |
| `references/library-context.md` | `933F4074AB3FFD71CD1221152F2C6BFBEEFE5E52BCCFDEBE5C326D0457704C7A` |

Paths above are relative to `example-workflows/evolve/` in the repository. Original artifact SHA-256: `F7A3F2916219609E97DA8BD91698E8D5773B6B408791475095A044BADDC14EE6`; h1: `7087F01C8820E21FBE6E02C3A4DFC81B81CE501C71FEF6E7272192F3DF5C6D4F`; initial evaluator prompt: `39754B97ED1983EC325B057969DC46049661AD55212DCB9164C6286AAB195507`.

## Observed execution

The coordinator verified the original artifact, h1 and v1 hashes, then appended a `resume` reconciliation event at 04:28:25 UTC. It advanced the checkpoint's last decision to `e0001`, retained a0/h1 and consumed exactly the one synthetic fixture round, leaving two. The next native experiment was `e0002`; no duplicate of the saved rejected candidate was dispatched. Since the fixture had no old live or unknown handle, this observes reconciliation without exercising native recovery.

The coordinator retained v1 after its own direct calibration and reserved the three cases before launching a fresh task-only proposer. `experiments/e0002/plan.md` records the hypothesis, same-model and work constraints, candidate ownership, unchanged evaluator, comparison rule and next ordinary round. The proposer record in `native-handles.json` specifies `fork_turns: none`. h2 adds one sentence to h1: preserve every source fact affecting a visitor's plan or service access even if the word target is exceeded. This is a real proposed harness revision, with no efficacy claimed from reading its prose. The coordinator froze it as unvalidated and started separate native h1/h2 makers on matched case source and starting drafts.

All six returned outputs were frozen with SHA-256 identities and screened. Every arm retained the practical facts, including the west ramp and operating lift that the synthetic failure omitted. The motivating h1 omission did not reproduce. Two fresh task-only judges inspected anonymous actual notices under unchanged v1. Private mappings randomized each pair's initial labels and reversed every pair for confirmation. The supervisor read both saved judge assignments and independently verified that all 12 anonymous A/B copies match the correct frozen output hashes. Neither assignment included origin, hypothesis or earlier verdicts.

| Matched case | First judge, decoded | Reversed-label judge, decoded | Requirements |
| --- | --- | --- | --- |
| Entrance access, known-failure category | h2 | h2 | Both arms pass |
| Book sale, synthetic retention anchor | tie | tie | Both arms pass |
| Printing service, fresh relative to history | h1 | h2 | Both arms pass |

The judges preferred h2's specific affected-entrance heading in the first case. Their printing preferences disagreed about grouping affected/unaffected services versus placing the workaround immediately after the outage. The coordinator retained h1 and journaled that decision before advancing the checkpoint to one remaining round. h2 remains an unadopted candidate with its actual outputs and failure evidence. This demonstrates comparison and conservative rejection; it does not demonstrate that the added fact-retention instruction caused the small heading preference, because one generation per arm cannot separate that effect from ordinary generation variability.

The fixture evaluator's phrase “no confirmed old preference” was weaker than the workflow reference's disagreement rule. The coordinator had already recorded “retain h1/a0 on ... contradictory evidence” in its pre-search resume notes and applied that rule. It disclosed the wording nuance in `experiments/e0002/evidence.md`; it did not loosen the evaluator after observing results. This is a fixture-policy ambiguity, not evidence of an evaluator improvement.

The harness experiment used nine native children: one proposer, six case makers and two comparison judges. All case makers reported one read and one write; the proposer reported two tool calls. The judges reported 11 and 14 individual read calls. Equal one-turn/tool allowances and inherited model settings were recorded; exact token and monetary costs are unavailable, so this is matched work allowance rather than demonstrated equal compute. The proposer reported reading only h1, the public evaluator and assigned core/guidance, not the reserved cases. Logged `fork_turns: none` removes inherited coordinator case context; shared-file access was not sealed.

The final ordinary maker received h1 explicitly in `experiments/e0003/assignments/maker.txt`, along with a0, v1, the source and a hypothesis about removing repeated closure wording and grouping visitor actions. Its actual output moved closure/reopening together and labelled books, holds and storytime. This verifies that the retained active harness reached a later maker assignment. It does not verify downstream use of an accepted new harness, since h2 was rejected.

The first fresh anonymous judge preferred this ordinary candidate; the reversed-label judge returned tie, citing comparable benefits in a0's dated heading and separate storytime paragraph. Both found all requirements satisfied. The coordinator retained a0, journaled `e0003` at 04:50:37 UTC, then saved a completed checkpoint with `last_committed_decision: e0003`, `remaining_bounds.rounds: 0`, `in_flight: []`, unchanged a0/h1/v1 identities and an explicit exhausted-bound stop reason. It preserved the unadopted h2 and unpromoted ordinary candidate as archive evidence. No background execution or further improvement was claimed.

The two resumed rounds used 12 native children total: one proposer, seven artifact makers and four independent judges. The final ordinary maker reported two tool calls, and its judges reported seven and one read commands. Coordinator, trial supervision and reporting are additional overhead; tokens and monetary cost remain unavailable. The checkpoint separately counts one synthetic fixture round and two newly observed rounds.

## Evidence locations

All paths below are relative to the disposable run directory `C:/Users/danhm/tmp/evolve-harness-20260913-0024/evolve-runs/notices/`. Raw run outputs remain outside the repository/package.

| Evidence | Location |
| --- | --- |
| Initial provenance and source | Project-root `AUTHOR-PREPARATION.md`, `briefs/main.md`; run `brief.md` |
| Reconciliation, calibration and pre-search rule | `resume-notes.md`, `journal.jsonl` |
| Evaluator, reserved cases and active harness | `evaluation/v1/`, `harness/h1/instructions.md` |
| Unadopted harness proposal and hypothesis | `harness/h2/instructions.md`, `experiments/e0002/proposal/`, `experiments/e0002/plan.md` |
| Six matched outputs, identities and private mapping | `experiments/e0002/outputs/`, `output-identities.json`, `private-mapping.json` within that experiment |
| Harness review assignments and actual judgments | `experiments/e0002/assignments/judge1.txt`, `judge2.txt`; `judge1-raw.md`, `judge2-raw.md`, `evidence.md` within that experiment |
| Later active-harness assignment and unpromoted notice | `experiments/e0003/assignments/maker.txt`, `experiments/e0003/candidate/notice.md` |
| Ordinary artifact judgments and decision | `experiments/e0003/judge1-raw.md`, `judge2-raw.md`, `evidence.md` within that experiment |
| Native handles and observed cost | `native-handles.json` in each new experiment; run `cost.json` |
| Final retained artifact and continuation state | `artifacts/a0/notice.md`, `checkpoint.json`, `checkpoint.previous.json`, `journal.jsonl` |

## Interventions and limits

No author intervention after coordinator dispatch occurred. The coordinator reported one failed attempt to save raw judge prose caused by PowerShell parsing a curly apostrophe; it retried with a literal here-string before any state or candidate damage. That was a local persistence repair, not an experiment or reviewer repair.

The trial has a hard two-round observation window; it cannot establish indefinite execution, sustained improvement, RSI Level 1, real crash recovery, native child recovery, unknown-liveness handling, wide tournaments, rollback, visual/audio judgment, or unseen-task generalization. The earlier checkpoint/journal decision is a prepared interruption, and its evidence is explicitly synthetic. Because h2 was rejected, downstream use and rollback of an accepted harness remain unexercised.
