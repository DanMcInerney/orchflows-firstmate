The pass inspects the supplied native history and current source, fixes the command in the owning workflow, and uses one maker and one independent reviewer. An improvement claim includes actual old and new behavior on the same input under matched conditions: the old command counts four physical lines; the corrected command counts two nonempty records.

The owning library or project receives a reusable regression request and expected behavior. Actual outputs, reports and transcript material stay in the caller workspace outside packages. Evidence identifies source versions, frozen assignments, commands, native child handles, outcomes, observed cost, interventions and unavailable history.

Compare observed baseline and candidate behavior without assuming the candidate wins. A missing or uninformative comparison limits the claim. This case does not exercise report-only requests, already-fixed history or environment-only corrections.
