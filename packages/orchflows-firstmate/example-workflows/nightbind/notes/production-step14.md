# Production preparation — step 14

The approved mechanical baseline remains `core-220cfa39951c` in `dist/` and on port 4191. Current source/dev on port 4190 contains the presentation preparation below. It is **not** the frozen core build, and no complete integrated asset build has been claimed yet.

## Shared import and representative inspection

`src/assets.js` owns the production GLTFLoader, manifest validation, measured import diagnostics, rigid hierarchy cloning and instanced part batches. It rejects missing/non-JSON manifests, HTML masquerading as GLB, unsupported binary headers, absent named roots and skinned meshes. It preserves exported node transforms and reports sockets, bounds, primitive/triangle/material costs, texture memory estimates and exported clips. Manifest hashes are compared with actual bytes. Shared resources are released explicitly.

`src/render-settings.js` provides the same camera, lights, ACES exposure, sRGB output and fog to game and viewer. `asset-preview.html` exposes asset, view and facing selectors, reload, ground warnings, socket axes and rigid motion. The read-only `#asset-observation` reports actual import details, renderer/backend, assembly bounds and the active camera. Its mixed/busy views use the production batch implementation, with 180 supplied models. This is inspection evidence, not a gameplay performance result.

The Blender maker owns all source models, exports, portraits, manifest and asset report. The shared runtime caught an actual representative defect before expansion: keeper socket rotation made the lantern horizontal and detached. The Blender maker corrected the source empty basis to export identity socket rotation. The loader does not compensate a second time. Corrected keeper/lantern, arch and joined rim were observed in the actual viewer by the caller and asset maker; the game maker inspected saved images. See `evidence/art-direction/keeper-glb-*` and `evidence/assets/`. Later mixed bases and evolved lineups were also inspected; final asset acceptance remains in the separate asset report.

The suspended bell exposed a viewer placement issue: its top-root and negative local Y bounds initially put it below the reference floor. The preview now raises the standalone piece and reports that inspection-only placement, and attaches it at the arch's exported `socket_bell`. Its correct suspension pivot is unchanged. The asset maker observed both corrected views; the game maker inspected `evidence/assets/hanging_bell-runtime-detail.png` and `arch-bell-runtime-assembly.png`. The bell is visible and connects beneath the arch support.

## Bounded content and feedback changes

`src/encounters.js` gives each of the twelve existing waves a short learning, pressure, combination, release or boss cue. The first two teach automatic lantern fire and ally choice; waves 3–4 teach movement and first ingredients; wave 5 points to awakening; waves 6–8 reinforce roles and deliberate interception; wave 9 signals fresh-ground pressure; wave 10 explains ascension; wave 11 gives breathing room or unfinished-recipe advice; wave 12 names the boss warnings. Cues appear briefly at wave starts and in the Field guide.

Capture readiness now draws the actual 13-unit reach circle. Hover/focus on a capture choice highlights the exact living hostile selected by that button. Companion attack effects carry source ID/type metadata; the visible beam starts at the acting companion, while the approved simulation damage and range remain centered exactly as before. Small source pulses identify area-attack ownership. These changes address CORE-O2 without granting captures or changing attacks.

An ordinary caller check found that the initial compact capture panel still obscured the keeper and target on the arena's right side. The corrected planning view uses a dedicated unobstructed left viewport, centers the keeper only during the public tactical pause, and fits the complete capture circle beside a stable right panel. The caption explicitly names this Lantern view. Closing restores the ordinary camera; the first subsequent ground input restores that camera before ray mapping. No simulated time, player position or capture rule changes when switching views.

The caller's bounded ordinary check **passed** on isolated `step14-ui-df36d6bac38a`, using public pause/tool pacing. Keeper and selected targets were visible at right x=11.66 (id 6), left x=-10.85 (id 5) and center x=-0.116,z=-0.003 (id 10), with the full reach circle in the 734px left viewport. Cancel followed by the first click at (420,355) reached x=-10.854,z=-0.317; Cancel followed by (640,360) reached center. Capturing id 10 removed the actual foe, added an Emberling and consumed the charge; ordinary auto-fire also killed another enemy before the paused observation. The game maker inspected the three images and state evidence in `evidence/caller/lantern-view-{right,left,center-isolated}.*`, `lantern-first-move.json` and `lantern-capture-after-correction.json`. No console errors were reported. This is caller-operated production QA, not final independent acceptance.

Two interrupted attempts are retained honestly. A dev attempt reloaded during concurrent source/export work, so the caller moved the check to an isolated compiled preview. A later concurrent-session attempt unexpectedly returned to title; an exclusive repeat did not reproduce it. Source has a Pause → Start a new run path, but the precise context of the earlier reset was not established and a new-tab focus probe did not prove that cause. Treat it as an unresolved tool-context observation, not a confirmed camera or game-reset defect. The final exclusive movement/cancel/capture sequence above passed. The temporary 4192 preview was then stopped; its compiled artifact is preserved unchanged.

CORE-O1's strong Worldcoil wave-11 recovery is retained as the earned relief already accepted at the core checkpoint. No spawn rates, health, damage, movement, charge cadence, recipes, wave timing, contact thresholds or boss rules were retuned in step 14.

## Verification and next join

Thirteen tests pass: the nine approved core regressions, source-effect metadata without changed damage center, rejection of invalid asset payloads, rigid part/entity transform parity with Three.js, and measured mesh/material/socket diagnostics. A Vite multipage compile of the game plus viewer succeeded in `evidence/stage14-build/`; it intentionally did not replace the approved `dist/`. The isolated UI candidate is fingerprinted as `step14-ui-df36d6bac38a` in `evidence/builds/step14-ui.json`. The later viewer-only bell correction is not included in that candidate and has its own actual dev-browser inspection above. The remaining Vite advisory is a shared Three.js chunk above 500 kB before gzip, not a load failure or measured performance finding. All three approved core runtime files were hash-verified and copied into `evidence/builds/core-220cfa39951c-runtime/` before any later joined build replaces `dist/`.

The maker's previously working browser binding currently reports no available browser. The supported setup path was retried; no alternate browser transport or injected page mutation was used. The caller and asset maker have working supported browser sessions and supplied observed runtime evidence. This limitation is explicit: the maker does not claim a new personal full playthrough of these presentation changes.

Step 14 is ready for the caller's asset join. After that join, the game still needs the authored models/portraits assembled into ordinary play, rigid animation and remaining feedback polish, integrated loading/error checks, affected ordinary/scenario play, exact production freeze and actual busy-game profiling. `production-qa-plan.md` defines the planned coverage. A complete Dusk → Eclipse ordinary run will broaden the earlier Solar and independently reviewed Worldcoil routes. Static viewer counts do not stand in for that final QA.
