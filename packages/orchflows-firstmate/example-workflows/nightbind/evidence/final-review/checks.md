# Final review verification record

Reviewer `/root/nightbind_final_review`, 2026-09-13. These checks followed the first public-only loss. The game, assets and compiled outputs were not modified.

- Independently ran `& 'C:/Program Files/nodejs/npm.cmd' test` from the game project once. Result: 13 tests, 13 pass, 0 fail, total 119.6004 ms. This is the recorded result of that review tool call, not copied maker output. Covered invalid asset response, rigid batching/import transforms, ally effect metadata, same-tick death/healing, real capture/charge, pause/release, wave/ingredient gates, second-evolution consumption, alternate combat routes, seed/reset, normalized movement/border and boss/outcome/retry. No new tests or rebuild were needed.
- `verify.py` independently hashed both complete candidate source/output sets, checked path-set equality, build digest, release/assisted diagnostic separation, asset manifest and all 28 joined handoff documents. `integrity.json` records zero mismatches. This script writes only this review directory.
- `inspect_blend.py` opened five saved Blender sources read-only: keeper assembly, arch/rim assembly, Storm, Solar, boss. Blender 5.2.1 loaded editable mesh objects, hierarchy and materials. `blender-source-inspection.json` contains the inventory. No source save/export occurred. All 24 saved sources are covered by file integrity; the other 19 were not individually opened by this reviewer. Existing all-22 re-export/validator/import evidence was inspected as maker evidence, not claimed as a new export.
- `performance-recalculation.json` independently recomputes the complete 1,322-frame reviewer and 1,321-frame caller wave-nine raw slices. It preserves separately labeled full-run/busy summaries from the reviewer's original browser export. `performance-wave9.json` is valid complete JSON, not a truncated transcript.
- Source review covered authoritative simulation/actions, pause/input/outcome handling, raw frame sampling, diagnostics boundary, camera/raycast mapping, instancing/rigid poses, shared lighting/color settings and disposal paths. Focus/context-loss recovery was source reviewed but not deliberately exercised.

Assisted checks used `qa-c09f13cac8e9`, seed 12345, visible scenario controls, manual clock, public recipe/capture/release actions. They are not ordinary progression or performance measurements. Missing-pair UI was observed both before wave 5 (51) and at wave 5 after releasing Thorn (53). Full roster 54 blocked capture. Releasing Fang and choosing Ember at the same tick changed six companions to five then six, 60 foes to 59, charge one to zero, with zero kills (55). Settings plus Step 60 left tick/time/charge unchanged (56). Storm/Dusk and World/Eclipse each consumed the supplied recipe and applied distinct damage by tick 90 (57,59–61); Storm additionally moved to x10 through tick 300 (58). Boss-danger survived initial steps, then lost at tick 839/time255.983 with zero health (62–64). Public retry reset stats/squad/health and resumed live mode (65).

Evidence interpretation corrections retained without renaming or erasing files:

- Ordinary images are captured immediately before public pause and state sampling; image/JSON pairs are nearby observations, not atomic measurements. The guide obscures the horde in gate images 18/36; use live images 17/34/35 for pressure.
- `50-clean-win-retry.jpg` captured a stale victory frame immediately after retry; its JSON is the actual fresh tick-zero paused state. `50b-settled-retry.jpg` is the subsequently settled pause UI.
- `52-missing-at-gate.*` actually records a stationary wrong-recipe fixture **loss before the gate**, at time86.8. It does not prove the missing-pair wave-five branch; 53 does.
- `63-boss-danger-outcome.*` is still playing at time254/9 health. The actual terminal loss is 64.
- `65-assisted-clean-retry.*` is already live at time0.35, tick21, one spawned foe. It establishes cleared old state, not tick-zero/manual parity.
- One settings-close locator assumed `#close-modal`, which that settings button does not have. It timed out without changing the game. A fresh DOM observation and the visible Return to the night button resolved it. Shell display of raw performance files was truncated; arithmetic used complete local parsed files, never that display.

The reviewer's ordinary retry deliberately chose one of each base role before Pyre, then a replacement Moth while keeping Volt for Solar. It stopped recruiting at roughly 94 seconds. A stationary test after Pyre caused substantial poison damage, followed by recovery after moving through fresh ground. Later it held center after Solar to challenge safety, then resumed lateral movement after observing the boss warning and projectile fan. This is adaptive pause-assisted ordinary play, not a precomputed input trace.
