# Forward trial

Run from an unrelated workspace using installed libraries or explicitly supplied core, export-workflow and social-search package roots. Give the runner the ordinary request below and available tool capabilities; keep [acceptance criteria](expected-behavior.md) separate.

> Use export-workflow to export social-search:social-search for the current Codex host into exports/social-search. Preserve all supported source scopes and keep runtime inputs configurable.
>
> Trial the exported skill on friction developers report adopting uv for Python project and dependency management. Research astral-sh/uv issues or discussions and Hacker News participants, with original issue/story dates from 2024-01-01 through the trial date. Use actual live web searches and inspect primary evidence, including relevant replies. Allow two collectors, at most six search/read targets per source, and at most three selected candidates per source. Count every batch target and retry separately; stop new acquisition six minutes after the research invocation starts. Evidence writing and final review may finish afterward. Return a concise cited assessment with dates, engagement when available, counterevidence and gaps. No supplied corpus or fixture.
>
> Then invoke the original social-search workflow on the same research brief and bounds in a separate fresh context. Give neither run the other's findings. Compare observed discovery, source inspection, orchestration and evidence quality; different source choices and rankings can be valid. Keep the skill, portability report and comparison evidence separate.

The original-workflow comparison is explicitly requested here in addition to the exporter's own trial and export review. These task-specific research bounds do not become exported defaults. Missing live access limits the validation claim; an offline substitute does not validate live retrieval.
