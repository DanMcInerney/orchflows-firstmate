# Library context

Resolve core `orch-work`, `orch-review`, `docs/architecture.md` and `docs/hosts.md` once through native skills, supplied package roots or core's `resolve` CLI. Core architecture owns guidance selection and model/effort choices; hosts owns invocation settings and isolation. Missing required core resources block dependent work.

Select `orchflows` and `writing` for export authoring and review, including this library and caller-selected libraries in supplied order. Read and apply Make while producing the export and pass the resolved Review guidance to its independent reviewer. Source-workflow guidance is source material to bundle; it does not replace the export author's guidance.

Pass resolved absolute paths, source identity, selected libraries, target host, caller choices and output locations unchanged to composed primitives. The exported skill's trial receives only its standalone folder, ordinary inputs and declared prerequisites, with fresh context and bundled guidance. Keep exports, reports and trial outputs in the caller's workspace, outside packages.
