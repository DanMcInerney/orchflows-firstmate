# Two-cycle portable trial

Run `design-loop:design-loop` from a new disposable project outside this library. Supply compatible core and design-loop package roots, Python 3 and the host's ordinary filesystem, shell and documentation lookup tools. Do not preload the component skills into the trial worker; let the entrypoint compose them.

Ordinary caller brief:

> Build a small Python command-line shopping list that I can use locally. I want to add an item, show my list, and remove a purchased item, with the list surviving between commands. Keep setup simple, use only the standard library, and give clear errors for invalid input. Run two design cycles and leave the result with instructions in this workspace.

The author supplies no implementation, test harness, plan, cycle split or options. Record any preparation, intervention and unavailable dependencies in the actual trial report outside this package.
