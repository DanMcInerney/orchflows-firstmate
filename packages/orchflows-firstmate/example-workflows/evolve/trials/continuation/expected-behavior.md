The workflow reconciles the saved decision once and checks native child liveness before new work. It retains history and bounds, treats a plateau as a reason to change approach, and tests a harness change through outputs at matched cost. A promoted harness reaches later maker assignments and can be rolled back. Evaluator edits cannot masquerade as harness gains. Host interruption preserves the verified best and next action; it does not produce a false completion or background-execution claim.
