# Request

Build a benchmark for the supplied coding workflow on the supplied pinned small project. Use four realistic change requests across two behavior families, including preserving existing behavior. Run at most two actual workflow executions with separate working copies and a ten-minute pilot budget. Grade delivered behavior with local checks; do not publish, deploy or install additional services. Keep output in this workspace.

Trial setup: provide a clean small project snapshot, public requirements, installed runtime and actual coding workflow invocation. Record revision/content identity. The workflow must receive a real change request rather than instructions to emit a canned patch.
