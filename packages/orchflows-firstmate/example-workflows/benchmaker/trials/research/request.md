# Request

Build a compact benchmark for the research workflow supplied in this workspace, using only the supplied frozen source corpus. Test synthesis, conflicting evidence and citation support. Keep the quick run under five minutes where feasible, with at most three actual workflow executions in the pilot and no paid judge calls. Record unsupported quality judgments instead of inventing them. Use the target's native invocation and keep output in this workspace.

Trial setup: supply an independently sourced compact corpus with provenance and at least one substantive contradiction, plus a real research workflow and its documented host invocation. Missing inputs are reported gaps, not permission to replace the workflow with canned text.
