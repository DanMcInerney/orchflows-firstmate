# Request

Build a benchmark for the supplied workflow that turns a short brief into a slide deck. Use the supplied briefs and source assets, with requirements for factual content, readability and audience fit. Author four development cases and run at most two actual workflow executions. Use installed renderers, no new paid services, and a ten-minute pilot limit. Keep output in this workspace and mark judgments you cannot perform.

Trial setup: supply the real workflow invocation, reusable briefs/assets, and at least one accessible example deck. Provide rendering/viewing access when available; deliberately missing modality access is a useful separate trial variant.
