# Trial acceptance

Apply the [shared acceptance](../expected-behavior.md), plus:

- All three requested scopes remain represented when grouping assignments. The feed set stays together. Per-scope read and candidate bounds survive grouping.
- Feed outcomes retain feed and entry URLs, separate published and updated times, and expose empty, inaccessible or truncated history. Exercise the generic reader when available; otherwise record that validation limit.
- Lemmy evidence identifies the serving instance and originating community and canonical identities for federated copies. Claims about participants rely on inspected comments; coverage remains limited to the instance's view.
- The reviewer groups duplicate underlying research across web, feeds and Lemmy, preserving access and date gaps. Search filters are not publication-date proof.
