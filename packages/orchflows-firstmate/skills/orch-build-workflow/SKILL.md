---
name: orch-build-workflow
description: Author and trial a reusable leaf workflow or guidance library through an admitted FirstMate dynamic local-only root.
---

**Conditional experimental execution:** use the [bounded authoring contract](../../docs/firstmate-client.md#bounded-leaf-authoring). First confirm the exact retained package and dynamic ship/local-only attachment with the client. This composition admits a leaf that performs its work without delegating, or guidance applied by such a leaf. General composing workflows, nested trials and SelfImprove remain unavailable; stop if they are required.

Name the recurring request and useful result. Follow [architecture](../../docs/architecture.md) for placement and dependencies. Use [orch-dynamic-workflow](../orch-dynamic-workflow/SKILL.md) with orchflows and writing guidance. Author a complete library in the assigned source worktree, with its inputs, output contract, dependencies and zero delegated agents declared. Preserve workflow model/effort preferences only when the user asks to save them; this session's settings are not library defaults.

Join and check the draft before a fresh Work component trials it from that exact clean commit. Supply the ordinary request, candidate library location, retained dependency roots and expected output location. The leaf resolves its own worktree paths, reads its full skill and required guidance, and performs the request without invoking Work, Review or fleet commands. Let its instructions determine the work; do not duplicate them in the trial assignment.

Read and gather the full trial result. Return findings to the authoring work before final Review, directly in the authorized root or in fresh scoped Work given the joined candidate and trial record. Simplify or repair and rerun affected behavior as needed before that Review. Commit the actual output and trial record outside the library, preserving the [distinct result and report digests](../../docs/firstmate-client.md#trial-digest-meanings); include candidate identity, declared inputs, resolved dependencies, checks, author preparation, intervention and unexercised behavior. A same-project trial establishes that bounded use, not portability.

Give the one final reviewer the exact clean candidate and committed trial evidence, then follow the dynamic repair/check pass and ordinary local-only delivery. If that pass changes a trialed library, use fresh Work to rerun the affected behavior and commit its actual output, retained result and trial record before delivery. Keep the original reviewed evidence and identify which repaired commit the additional trial covers; do not request another Review. On relaunch, reread this skill, inspect retained requests and committed trial evidence, and reapply the remaining artifact and validation requirements. Preserve accepted work; do not repeat an unchanged trial whose result is already retained or replace uncertain work.

For a portability claim, the outer caller must arrange an unrelated project with an ordinary brief and declared dependencies through FirstMate's existing enablement and launch owners, and return that trial evidence before final Review. Later reuse is separate evidence. Loading source paths does not register a skill by name, and delivery never implicitly installs the library into a user home.

## Upstream migration baseline (inactive)

The quoted original instructions below are retained for comparison; their native-child behavior does not apply to this fork.

> Name the recurring request and useful result. Follow [architecture](../../docs/architecture.md) for placement and dependencies. Use [orch-dynamic-workflow](../orch-dynamic-workflow/SKILL.md) with `orchflows` and `writing` guidance.
>
> Preserve [model and effort preferences](../../docs/architecture.md#model-and-effort) beside assignments when the user asks the resulting workflow to use them; otherwise leave them unspecified.
>
> Its verification includes a trial before final review. Run a workflow on a bounded request in a disposable workspace; for portable workflows, use an unrelated project, an ordinary brief and declared dependencies. Let the workflow supply the orchestration. For guidance alone, apply it to representative work that exercises the intended preference. Record author preparation, intervention and unexercised behavior as trial limits.
>
> Return trial findings to the maker before final review; simplify or repair and rerun affected behavior as needed. Give the final reviewer the candidate and trial record. Check [host registration](../../docs/hosts.md#register-and-refresh) before claiming a skill is available by name.
