diff --git a/AGENTS.md b/AGENTS.md
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -339,7 +339,7 @@
 The selected delivery path owns its own rigor.
 Once no-mistakes validation starts, no-mistakes alone owns review, fixes, tests, documentation, push, PR, and CI. Otherwise follow the selected delivery path without adding an independent reviewer, except for the explicitly selected workflow below.
 Never hold work outside no-mistakes for a manual clean verdict, stack serial manual reviews, or infer authority for one from security, architecture, or risk alone.
-A separate review or audit is allowed when the captain explicitly requests that deliverable, the authorized task is a knowledge-only review, or FirstMate has explicitly attached the dynamic Orchflows workflow with workflow-review policy to a Linux scout or an explicitly selected ordinary ship/local-only root. That selected workflow may compose scoped Work in FirstMate worktrees, join and check the candidate, and request one fresh read-only Review followed by one repair/check pass. It must not stack reviews or create another delivery gate; only the root follows its immutable ordinary scout-report or ship/local-only ready-branch delivery. Workflow enablement is not authority for FirstMate self-development delegation, promotion, or work after no-mistakes takes validation custody. One named audit question remains scoped to that question.
+A separate review or audit is allowed when the captain explicitly requests that deliverable, the authorized task is a knowledge-only review, or FirstMate has explicitly attached the dynamic Orchflows workflow with workflow-review policy to a Linux scout or an explicitly selected ordinary ship/local-only root. That selected workflow may compose scoped Work in FirstMate worktrees, join and check the candidate, and request one fresh read-only Review followed by one repair/check pass per dynamic invocation explicitly authorized by its retained composition. Loading a skill stays in the caller's context. Multiple calls require distinct retained call IDs; workers cannot declare additional calls or repeat a call's Review. A writable root Work assignment may delegate one level only when its request ID is a named caller in that composition, sharing the root's component capacity and returning all results through FirstMate owners. It must not stack reviews within a call or create another delivery gate; only the root follows its immutable ordinary scout-report or ship/local-only ready-branch delivery. Workflow enablement is not authority for FirstMate self-development delegation, promotion, or work after no-mistakes takes validation custody. One named audit question remains scoped to that question.
 If fast-path risk needs more rigor, escalate whether to use no-mistakes instead of inventing a manual gate.
 The path's worker, automated gates, and captain approval remain authoritative:
 
diff --git a/bin/fm-dod-lib.sh b/bin/fm-dod-lib.sh
--- a/bin/fm-dod-lib.sh
+++ b/bin/fm-dod-lib.sh
@@ -35,7 +35,7 @@
 When this task works on Firstmate itself, the repository root `AGENTS.md` (also imported by `CLAUDE.md`) is the primary/secondmate supervisor's contract: follow this brief instead of that supervisor contract.
 For that Firstmate task, do the assigned work yourself and report to firstmate; do not adopt the supervisor identity, delegate the task, run fleet supervision, or address the captain.
 This exception preserves this brief's safety and authority boundaries and applicable contributor guidance, including `CONTRIBUTING.md` and `firstmate-coding-guidelines` for Firstmate changes.
-Other projects retain their own instructions unchanged. An explicit FirstMate dynamic attachment may authorize scoped Work components, one fresh read-only Review, and one repair/check pass through FirstMate owners. Only the root follows its immutable ordinary scout-report or ship/local-only ready-branch delivery; components return to their parent. This does not authorize delegation for Firstmate self-development, promotion, or a separate delivery gate.
+Other projects retain their own instructions unchanged. An explicit FirstMate dynamic attachment may authorize scoped Work components, one fresh read-only Review, and one repair/check pass per authorized dynamic call through FirstMate owners. A retained selected composition may name several caller-context calls and one level of delegation by named writable Work assignments; it grants no unrestricted fleet authority. Complete and gather every authorized descendant call before returning the caller's result. Once no-mistakes validation starts, it alone owns validation and delivery; no additional Work or Review is authorized. Only the root follows its immutable ordinary scout-report or ship/local-only ready-branch delivery; components return to their parent. This does not authorize delegation for Firstmate self-development, promotion, or a separate delivery gate.
 EOF
 }
 
